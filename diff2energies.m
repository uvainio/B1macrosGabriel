function diffint = diff2energies(data,param,sample,energies)

% function diffint = diff2energies(data,param,sample,energies)
%
% Created 7.11.2007 UV

sd = size(data);
for(k = 1:sd(2))
    if(strcmp(param(k).Title,samplename) & )
    
    loglog()
end;